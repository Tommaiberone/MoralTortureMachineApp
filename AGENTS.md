# Agent Instructions

Before making changes in this repository, read and follow `CLAUDE.md` in full.
It is the authoritative repository instruction file.

Product delivery is managed with Backlog.md. Before starting work, also read
`backlog/docs/doc-1`, `backlog/decisions/decision-1`, and run
`backlog task list` to find the relevant task. Product strategy and validation
gates live in `backlog/docs/doc-2`.
