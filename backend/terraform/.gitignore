# Terraform files
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!terraform.tfvars.example
.terraform.lock.hcl
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Lambda build artifacts (these are built during terraform apply)
../lambda_deployment/
../lambda_function.zip

# Sensitive files
.env
*.pem
*.key
